# Durable insights

## 2026-09-14

Upwork MCP `find_jobs` with `action=search` is blocked for the connected freelancer account (TOS restriction). Hourly keyword demand tracking cannot collect marketplace results until search access is restored. `smart_search` (best_match / most_recent) responds OK but returns no jobs (`no_personalization` / empty feeds).
