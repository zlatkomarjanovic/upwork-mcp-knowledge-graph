#!/usr/bin/env python3
"""Placeholder: MCP calls must be made by the cloud agent via Upwork namespace."""
import json
import sys

print(json.dumps({"status": "error", "error_code": "NO_BRIDGE", "message": "Use agent CallDynamicTool"}))
sys.exit(1)
